stage('test-examples') {

    if (env.SGX == "1") {
        env.MODE = "sgx"
    }
    else {
        env.MODE = "direct"
    }

    if (["ubuntu", "debian"].contains(env.os_release_id)) {
            try {
                  sh '''
			cd CI-Examples/mysql
                        #sudo make uninstall-mysql && make clean
			sudo -E make install-dependencies && make privileges
                        make ${MAKEOPTS} check 2>&1 | tee RESULT
                        make conncheck
			sudo make uninstall-mysql
                    '''
         } catch (Exception e) { }
     }
}
